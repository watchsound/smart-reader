// Phase 9a placeholder — Phase 9b will source from Call Ledger
// once Phase 6 ComprehensionGradingService is migrated through brainCall.
const BrainContext = require('../BrainContext');

BrainContext.registerSlice('recentComprehension', async () => {
  return [];
});
