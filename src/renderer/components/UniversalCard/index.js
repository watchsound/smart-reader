/**
 * UniversalCard Component Exports
 *
 * Unified card component for all learning content types:
 * - Vocabulary (simple text flip)
 * - Notes (text, quiz, mindmap, image)
 * - Learning Plan items (STEM with formula support)
 */

export { default as UniversalCard } from './UniversalCard';
export { default as CardContentRenderer } from './CardContentRenderer';
export { default as ScratchCanvas } from './ScratchCanvas';

// Re-export for convenience
export { default } from './UniversalCard';
